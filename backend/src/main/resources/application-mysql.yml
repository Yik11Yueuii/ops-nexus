spring:
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://localhost:3306/opsnexus}
    username: ${MYSQL_USER:opsnexus}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never

